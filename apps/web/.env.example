OPENAI_API_KEY=
EXEC_API_URL=http://localhost:8080/api/exec

# Docs viewer - points to the SSH server's HTTP API
# Leave empty to use relative URLs (works when web app is served by the SSH server)
NEXT_PUBLIC_EDITOR_API_URL=http://localhost:8080
NEXT_PUBLIC_EDITOR_API_KEY=

# Optional - rate limiting skipped if missing
KV_REST_API_URL=
KV_REST_API_TOKEN=
